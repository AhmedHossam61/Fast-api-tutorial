# Fast-api-tutorial

## installation
you can istall all fastApi dependancies with 
```bash
pip install fastapi["all]
```

or use with mininum installation with
```bash
pip install fastapi uvicorn[standard]
```
